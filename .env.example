# Required
OPENAI_API_KEY=sk-REPLACE_ME

# Use an OpenAI Realtime model that supports speech-to-speech
# Example name shown; confirm your available model in your OpenAI project if needed
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview

# Optional: restrict Twilio websocket connections (recommended)
# Set this to your Twilio Account SID or leave blank to disable check
TWILIO_ACCOUNT_SID=
# Required: Twilio Auth Token for signature validation
TWILIO_AUTH_TOKEN=

# Optional: your public base URL (for generating TwiML stream URL)
# If not set, app will infer from request host
PUBLIC_BASE_URL=

# Server
PORT=8080
LOG_LEVEL=info

# Voice and Model configuration
# Set the voice for AI responses (e.g. ember, alloy, etc.)
VOICE=ember
# Optionally override the OpenAI Realtime model (e.g. gpt-4o-realtime-preview)
# OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview
# Optional: model for translating tool payloads to English
# OPENAI_TRANSLATE_MODEL=gpt-4o-mini
# Optional: model for KB embeddings
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Knowledge base ingestion/search settings
# KB_INDEX_PATH=./data/kb_index.json
# KB_REFRESH_ON_START=false
# KB_MAX_PAGES=80
# KB_MIN_CHARS=300
# KB_CHUNK_SIZE=1200
# KB_CHUNK_OVERLAP=200
# KB_EZ_BASE_URL=https://ezlumperservices.com/
# KB_HAULPASS_HOME=https://haulpass.ezlumperservices.com/
# UNKNOWN_QUESTION_LOG_PATH=./data/unknown_questions.log

# CRM / GHL integration
# Personal access token for GoHighLevel (PIT)
GHL_PIT_TOKEN=
# Location ID for your GoHighLevel account
GHL_LOCATION_ID=
# Webhook URL for new job intake submissions
WEBHOOK_NEW_ORDER=
# Webhook URL for existing order updates
WEBHOOK_EXISTING_UPDATE=
# Webhook URL for callback requests
WEBHOOK_CALLBACK_REQUEST=
# Webhook URL for transfer to agent requests
WEBHOOK_TRANSFER_REQUEST=
# Webhook URL for unknown question logging (optional)
WEBHOOK_UNKNOWN_QUESTION=
